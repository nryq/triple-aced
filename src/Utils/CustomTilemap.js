
import Phaser from "phaser";

export default class CustomTilemap extends Phaser.Tilemaps.Tilemap{
	constructor(scene, config){
		super( scene, config )
		// this.layers = [];
		// this.tilesets = [];
	}

	// addTilesetImage(tilesetName, key, tileWidth, tileHeight, tileMargin, tileSpacing, gid){
		
		
	// 	this.buildMap();
	// 	return tileset;
	// }

	// buildMap(){}
}