import Phaser from "phaser";

export default class TASprites{
	/**
	 * 
	 * @param {*} scene 
	 * @param {*} x 
	 * @param {*} y 
	 * @param {*} texture 
	 * @param {*} frame 
	 */
	constructor(scene, x, y, texture, frame){
		// console.log('scene.sys', scene)
		// super(scene, x, y, texture, frame)
		// scene.add.existing(this);
		// this.facing = null;
	}
	
}